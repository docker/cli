# manifest rm

<!---MARKER_GEN_START-->
Delete one or more manifest lists from local storage


<!---MARKER_GEN_END-->


Local lists live under `$DOCKER_CONFIG` (default `$HOME/.docker`). Using `sudo` often points at a different store — see [docker manifest](manifest.md).
